import 'package:flutter/material.dart';

class CreateUpdatePage extends StatelessWidget {
  const CreateUpdatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Create Update'),
      ),
      body: const Center(
        child: Text('Create standup update - Coming soon'),
      ),
    );
  }
}